/**
 * Hero Section
 * 
 * The hero section with calculator for the cleaning service landing page
 */

import React from 'react';

export default function Hero() {
  return (
    <section id="hero">
      {/* Hero content will be added by the user */}
    </section>
  );
}
